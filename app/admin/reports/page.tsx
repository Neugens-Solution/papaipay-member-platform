import { Card, KpiCard, PageHeader } from "@/components/admin/AdminUI";import { adminKpis, reportGroups } from "@/lib/adminMockData";
export default function ReportsPage(){return <div className="mx-auto max-w-7xl space-y-6"><PageHeader eyebrow="Reports" title="Reports & Analytics" description="Member, participation, listing and distribution reporting with placeholder export actions."/><section className="grid grid-cols-2 gap-3 lg:grid-cols-4">{adminKpis.slice(0,4).map(k=><KpiCard key={k.label} {...k}/>)}</section><section className="grid gap-4 lg:grid-cols-2">{reportGroups.map((g,i)=><Card key={g}><div className="flex items-start justify-between gap-3"><div><h2 className="text-lg font-bold">{g}</h2><p className="mt-2 text-sm leading-6 text-slate-500">Analytics summary, trend chart placeholder and filters for {g.toLowerCase()}.</p></div><button className="rounded-md border border-slate-200 px-3 py-2 text-xs font-bold text-slate-600">Export</button></div><div className="mt-6 flex h-40 items-end gap-2">{[42,65,51,74,60,88].map((h,j)=><div key={j} className="flex-1 rounded-t-lg bg-papaipay-green/70" style={{height:`${h-(i*4)}%`}}/>)}</div></Card>)}</section></div>}
